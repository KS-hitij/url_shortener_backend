import express from "express";
const router = express.Router();

//route for sign up
router.post("/signup",);

//route for sign in
router.post("/signin",);

//route for profile info
router.get("/profile",);